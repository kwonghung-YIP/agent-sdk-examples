[project]
name = "fastmcp-examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=4.0.3",
    "httpx[http2]>=0.28.1",
]

[project.scripts]
fastmcp-examples = "fastmcp_examples:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
